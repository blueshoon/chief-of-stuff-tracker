{
  "name": "chief-of-stuff-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -c tailwind/tailwind.config.js -i tailwind/input.css -o public/assets/app.css --minify",
    "watch": "tailwindcss -c tailwind/tailwind.config.js -i tailwind/input.css -o public/assets/app.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"
  }
}
